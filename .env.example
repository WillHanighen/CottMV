# CottMV Environment Variables
# ============================
# Copy this file to .env and fill in your values

# Server Configuration
# --------------------
# Port to run the server on
PORT=3000

# App URL (used for OAuth callbacks)
APP_URL=http://localhost:3000

# Convex Configuration
# --------------------
# run bunx convex dev --once

# GitHub OAuth Configuration (Required for authentication)
# --------------------------------------------------------
# Create a GitHub OAuth App at: https://github.com/settings/developers
# Set the callback URL to: http://localhost:3000/auth/callback
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Cloudflare R2 Configuration (Optional)
# --------------------------------------
# These can also be configured via the admin UI
# R2_ACCESS_KEY_ID=your-access-key-id
# R2_SECRET_ACCESS_KEY=your-secret-access-key
# R2_BUCKET_NAME=your-bucket-name
# R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com

# External API Keys (Optional - for metadata fetching)
# ----------------------------------------------------
# TMDB_API_KEY=your-tmdb-api-key
# LASTFM_API_KEY=your-lastfm-api-key
